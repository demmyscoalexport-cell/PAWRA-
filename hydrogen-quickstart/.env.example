/**
 * ╔═══════════════════════════════════════╗
 * ║          PAWRA PET SHOP               ║
 * ║    Premium Pets Products Store        ║
 * ║         pawrapetshop.com              ║
 * ║          © 2025 Pawra LLC             ║
 * ╚═══════════════════════════════════════╝
 */

# PAWRA Hydrogen — environment variables
# Copy to .env in this folder before local dev. Never commit .env.

# ─── Session ────────────────────────────────────────────────────────────────
# Required — random string for session encryption (server-side only)
SESSION_SECRET=""

# ─── Shopify Storefront API (Admin → Sales channels → Headless) ─────────────
# Public token — safe for client-side use via Hydrogen storefront client
PUBLIC_STOREFRONT_API_TOKEN=""
PUBLIC_STORE_DOMAIN="pawrapetshop.myshopify.com"
PUBLIC_STOREFRONT_ID="1000148769"
# Private token — server-side only, never expose to the browser
PRIVATE_STOREFRONT_API_TOKEN=""

# ─── Checkout ───────────────────────────────────────────────────────────────
PUBLIC_CHECKOUT_DOMAIN="pawrapetshop.myshopify.com"

# ─── Customer Account API (optional — /account login & orders) ────────────
PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID=""
PUBLIC_CUSTOMER_ACCOUNT_API_URL=""

# ─── Webhooks (optional) ────────────────────────────────────────────────────
# Server-side HMAC verification secret — never expose
SHOPIFY_WEBHOOK_SECRET=""

# ─── Meilisearch Cloud (optional — fast Chewy-style search) ─────────────────
# Host is shared by server and client; search key is safe for the browser.
MEILISEARCH_HOST=""
MEILISEARCH_MASTER_KEY=""
MEILISEARCH_ADMIN_KEY=""
MEILISEARCH_READONLY_ADMIN_KEY=""
PUBLIC_MEILISEARCH_SEARCH_KEY=""
# Reserved for future AI shopping assistant — do not wire unless needed
MEILISEARCH_CHAT_API_KEY=""

MEILISEARCH_PRODUCTS_INDEX="products"
MEILISEARCH_COLLECTIONS_INDEX="collections"
MEILISEARCH_BRANDS_INDEX="brands"
MEILISEARCH_CATEGORIES_INDEX="categories"
MEILISEARCH_ARTICLES_INDEX="articles"
MEILISEARCH_PAGES_INDEX="pages"
MEILISEARCH_SUGGESTIONS_INDEX="suggestions"
