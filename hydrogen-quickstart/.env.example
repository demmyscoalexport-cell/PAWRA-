/**
 * ╔═══════════════════════════════════════╗
 * ║          PAWRA PET SHOP               ║
 * ║    Premium Pets Products Store        ║
 * ║         pawrapetcares.com             ║
 * ║          © 2025 Pawra LLC             ║
 * ╚═══════════════════════════════════════╝
 */

# PAWRA Hydrogen — environment variables
# Copy to .env in this folder. Never commit .env.
#
# After creating a NEW Shopify store:
#   cd hydrogen-quickstart
#   npx shopify hydrogen link
#   npx shopify hydrogen env pull

# ─── Session ────────────────────────────────────────────────────────────────
SESSION_SECRET=""

# ─── Shopify Storefront API (Hydrogen channel → your storefront → API) ───
PUBLIC_STOREFRONT_API_TOKEN=""
PUBLIC_STORE_DOMAIN="pawrapercares.com"
PUBLIC_STOREFRONT_ID=""
# Private token — server-side only, never expose to the browser
PRIVATE_STOREFRONT_API_TOKEN=""

# ─── Checkout ───────────────────────────────────────────────────────────────
PUBLIC_CHECKOUT_DOMAIN="pawrapercares.com"

# ─── Customer Account API (/account login & orders) ─────────────────────────
PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID=""
PUBLIC_CUSTOMER_ACCOUNT_API_URL=""

# ─── Webhooks (optional) ────────────────────────────────────────────────────
SHOPIFY_WEBHOOK_SECRET=""

# ─── Admin API (server-side only — custom app) ──────────────────────────────
# read_products, write_products — never expose to the browser
SHOPIFY_ADMIN_API_TOKEN=""

# ─── Cloudinary (hero images & marketing media) ─────────────────────────────
PUBLIC_CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_URL=""

# ─── WaveSpeed AI (image-to-video for hero carousel, server-side only) ─────
WAVESPEED_API_KEY=""

# Mirror all values above in Shopify Admin → Hydrogen → Environment variables
# for Oxygen production. Local .env is not deployed automatically.
