{
  "name": "pawra",
  "version": "1.0.0",
  "private": true,
  "description": "PAWRA — one platform for dogs and cats (Shopify Hydrogen storefront)",
  "scripts": {
    "dev": "node scripts/run-store.mjs dev",
    "build": "node scripts/run-store.mjs build",
    "preview": "node scripts/run-store.mjs preview",
    "lint": "node scripts/run-store.mjs lint",
    "codegen": "node scripts/run-store.mjs codegen",
    "install:store": "npm install --prefix hydrogen-quickstart"
  },
  "engines": {
    "node": "^22 || ^24",
    "npm": "^8.0.0"
  }
}
